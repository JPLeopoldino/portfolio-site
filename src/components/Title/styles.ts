import styled from "styled-components";
import colors from "@/styles/colors";

export const Title = styled.h2`
  font-style: normal;
  font-weight: 300;
  font-size: 64px;
  line-height: 77px;
  color: ${colors.title};
`;
